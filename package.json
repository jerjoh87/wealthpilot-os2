{
  "name": "wealthpilot-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "next": "^14.2.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "@supabase/supabase-js": "^2.43.0",
    "plaid": "^25.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.3.0",
    "typescript": "^5.4.0"
  },
  "notes": {
    "ai": "Anthropic called via fetch server-side — API key never exposed to client",
    "future": [
      "plaid — add when connecting bank accounts",
      "snaptrade-node — add when connecting portfolio"
    ]
  }
}
